INCLUDE(MNG)

SET_SCOPE(CTCACHE_EXECUTABLE ctcache)
if(NOT TARGET ${CTCACHE_EXECUTABLE})
	MNG_ADD_PACKAGE(
		NAME CTCACHE
		GITHUB_REPOSITORY matus-chochlik/ctcache
		GIT_TAG main
		DOWNLOAD_ONLY
	)
endif()